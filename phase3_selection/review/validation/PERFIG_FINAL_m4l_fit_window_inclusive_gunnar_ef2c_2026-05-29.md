# Per-Figure Validation

Figure: `m4l_fit_window_inclusive`

Result: PASS

Checks:
- Stack/data readability: PASS. The stacked components and data points are distinguishable, and the pull panel remains legible.
- Pull panel: PASS. Pull markers and error bars are visible, centered on the zero line, and not clipped.
- Label compliance: PASS. Axis labels include units where applicable, and the visible labels use publication-style names rather than code identifiers.
- No title-like text: PASS. No plot title is present; the CMS/open-data label treatment is consistent with the project standard.
- Legend overlap: PASS. The legend sits in open space and does not visibly overlap the plotted content.
- Axis units: PASS. The main axis is labeled `m4l [GeV]`, and the y-axis is labeled `Events`.
- Caption/metadata coherence: PASS. The image matches `phase3_selection/outputs/FIGURES.json` for `id = m4l_fit_window_inclusive`, `category = inclusive`, `window = fit_window`, and the caption description matches the rendered content and use as a fit-window handoff figure.

Final verdict: PASS
